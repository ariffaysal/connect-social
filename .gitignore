node_modules
**/node_modules
dist
.next
# Runtime image uploads (backend/uploads). NOT backend/src/uploads, which is
# a source module that must stay tracked (builds on Vercel fail without it).
backend/uploads
.env
.env.local
.env.*.local
!.env.example
.DS_Store
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Freebuff tooling
.freebuff/

# Vercel
.vercel/

# video production artifacts
.video/
ConnectSocial-Promo.mp4
