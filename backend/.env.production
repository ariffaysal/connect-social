# ConnectSocial Backend - Production Environment
# These variables should be set in the Vercel dashboard for the backend project
# Do NOT commit actual secrets to this file - it's for reference only

# =============================================================================
# DATABASE (PostgreSQL via Neon)
# =============================================================================
DATABASE_URL=postgresql://neondb_owner:npg_N0t5BaxYlrTz@ep-jolly-sun-azftyi1j-pooler.c-3.ap-southeast-1.aws.neon.tech/connect_social?sslmode=require&channel_binding=require

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# Must be at least 32 characters. Generate with: openssl rand -base64 48
JWT_SECRET=tMJtOZPGylKrhwd/1ovALtXwp8KCXjDUGFskEXHs3EtUzw5t3T8Viexf1TNt6Cmy

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed frontend origins
# Include both your deployed frontend AND localhost for development
CORS_ORIGIN=https://connect-social-five.vercel.app,http://localhost:3000,http://localhost:3001

# =============================================================================
# SERVER CONFIG
# =============================================================================
PORT=3001

# Set to false in production to use migrations instead of auto-sync
# For now, keep true for automatic schema updates (ok for small deployments)
DB_SYNCHRONIZE=true

# =============================================================================
# NOTES
# =============================================================================
# - WebSocket (/ws) is NOT supported on Vercel serverless functions
# - For full real-time features, deploy backend to Railway/Render/VPS
# - Uploaded images are stored in /uploads directory (needs persistent storage)
# - Set DB_SYNCHRONIZE=false and use migrations for production stability
